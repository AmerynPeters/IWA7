const value = 3;
console.log(value + 4 + value);
// Why it doesn't work: 3 is assigned as a string instead of a numerical, 
//so it will be concatenated as a string instead of added to the number 4 using the + operator.